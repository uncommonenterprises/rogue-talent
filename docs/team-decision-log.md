# Rogue Talent — team decision log

Maintained by the Product Manager. One dated line per decision: **what** was decided, **why**, and
whether it was **escalated** to Neil. This is how Neil audits the team without reading every session.

| Date | Decision | Why | Decided by | Escalated? |
|---|---|---|---|---|
| 2026-09-17 | Agent team stood up (PM lead + Developer + UX Tester + UX Designer) | Run development under Neil's direction | Neil | — |
| 2026-09-17 | Four briefs + charter approved as-is; models opus (PM/Dev), haiku (Tester), sonnet (Designer) | Stage 3 sign-off | Neil | — |
| 2026-09-17 | Pilot: UX Designer delivered `docs/design/dob-field-spec.md` (DOB signup field); accepted after PM review (in-bounds, no src/ touched) | Prove the reporting chain on a low-risk task | PM | No (low-risk design doc) |

| 2026-09-17 | Age gate SIGNED OFF by Neil; on origin/main + test deploy. NOTE: reached origin early via a stacked push before sign-off (test-only, no harm) — fix: isolate held-for-sign-off commits, don't push over them | Safety/legal sign-off (SAF-38) | Neil | Yes (safety) |

| 2026-09-17 | Journey 3 verified by PM: tester's "date-picker blocker" was a FALSE ALARM (automation artifact, not a defect); 15% customer fee confirmed correct (£300→£45→£345). Found availability-plan seed bug (seed wrote `availability-plan/day`; app produces `/time`) → fixed Anais/rt-model-03 listing in place + corrected seed script (commit 2b73928a7). | Unblock the booking money-path test | PM (Developer executed) | No (test data + dev-tool fix) |
| 2026-09-17 | Confirmed booking requires an email-verified client — `initiateTransactions` is gated on email verification (approved/active is not enough). Intended behaviour. | Access-control / product policy | Neil | Yes (product) |

| 2026-09-18 | Booking-v2 cancellation UI (increment 2) built + PM-reviewed — local commit 30ec2122d, NOT pushed. Structure/refund-logic approved. BLOCKING safety issue: provider safety-cancel reasons are written to client-visible transaction `protectedData` and the copy promises private team review + no reliability impact, neither of which is built. Must fix before ship (rec: interim generic-cancel for safety reasons; build private routing at go-live, ties to SAF-29/31/32). See docs/booking-v2-cancel-ui-review-2026-09-18.md. | Safety review of booking-v2 build | PM (flagged for Neil) | Yes (safety — pending Neil) |

| 2026-09-18 | Booking-v2 dispute UI (increment 3) built + PM-reviewed — local commit 744791f98, NOT pushed. Client dispute action (`completed`→`disputed-hold`) + state displays; scope correct (no in-app operator controls); tests 210/210. Required a NEW `transition/dispute` in the EDN (process change → needs Neil's approval). Design calls for Neil: client can unilaterally freeze payout; dispute window may be short. See docs/booking-v2-cancel-ui-review-2026-09-18.md. | Booking-v2 dispute increment | PM (Developer built; flagged for Neil) | Yes (process/money — pending Neil) |

| 2026-09-18 | ✅ END-TO-END MONEY PATH VERIFIED (test mode). Client rt-client-01 booked rt-model-03 (Anais) → Stripe test card → transaction 6aad6151 created on default-booking v1: lastTransition confirm-payment, payin £345 / payout £300 / fee £45 (15%). First real money movement. Client transaction rights were granted via Integration API (Neil ran the command; email-verification gate is the real mechanism). | Verify the booking money path | PM (Neil ran the grant) | No (test-mode verification) |

| 2026-09-18 | Booking-v2 decisions (Neil, via decision cards): (1) SAFETY cancel reasons — INTERIM: do a plain cancel, store nothing client-visible, direct the model to a safety-report channel; build private operator-only routing before go-live (SAF-29/31/32). (2) APPROVE client disputes (new `transition/dispute`) — client can pause payout until ops resolves. (3) Payouts: KEEP FAST (operator-release + P5D backstop), NO minimum dispute hold (preserves 'usually the next working day'). | Booking-v2 safety/process/money design | Neil (accepted PM recommendations) | Yes (safety/money) |

| 2026-09-18 | Safety-report channel = **safety@roguetalent.co** (Neil, decision card). Already the placeholder in the code (commit 7f381d127 safety-reason fix), so NO code change. Neil ACTION before go-live: create + monitor that inbox (copy promises 'a real person will read it'). Also flagged: SAF-29 proper safety-reporting flow still unbuilt — needed before booking-v2 goes live (until then a safety concern only reaches ops if the model emails). | Booking-v2 safety flow | Neil | Yes (safety) |

| 2026-09-19 | Alias push APPROVED by Neil (decision card) — push booking-v2 over the default-booking alias on TEST. Prereq found before push: booking-v2 has NO templates/ dir; its EDN references 11 notification templates, all STOCK default-booking names (which exist locally). So: assemble templates/ (copy stock) + re-validate EDN, THEN push. Developer preparing templates + exact runbook; Neil runs the flex-cli push (guided); live verification pending Playwright reconnect. | Booking-v2 go-live on test (money/process) | Neil approved; PM executing | Yes (money/process) |

| 2026-09-19 | ✅ booking-v2 LIVE on test — Neil pushed version 2 of default-booking + updated the `release-1` alias to point at it (flex-cli). `process list` confirms release-1 → v2 (0 tx yet); old v1 retains the 2 prior test tx. Cancellation/dispute system now active for new model-profile bookings. Behavioural verification pending Playwright reconnect. | Activate booking-v2 (money/process) | Neil ran the flex-cli push+alias (PM-guided) | Yes (money/process) |

| 2026-09-19 | Booking-v2 notification emails BUILT (commit 863853d16, pushed to git; NOT yet flex-cli-pushed) — 23 party-facing notifications (16→39) + 8 templates for cancels/disputes/backstop; PM-reviewed, v1 copy defaults accepted. NOTE: repo EDN now = intended NEXT process version; LIVE is version 2 (no cancel/dispute emails) until a future flex-cli push. KEY before-go-live item: operator-alert-on-dispute CANNOT be a process notification (Sharetribe `:to` = customer/provider only) → needs an events-listener/webhook (backend) so a dispute reaches ops (safety@). | Booking-v2 notifications | PM accepted copy; operator-listener flagged for Neil | Yes (before-go-live safety build) |

| 2026-09-19 | ✅ booking-v2 VERIFIED end-to-end on test (PM via Playwright). New booking tx 6aae5a9b on processVersion 2; full lifecycle request-payment→confirm-payment→accept (captured £345)→provider-cancel; client FULLY REFUNDED (payin £345→£0, payout £0); cancel UI reason-picker (reliability/safety) + refund preview render correctly; SAFETY fix confirmed — safety cancel stored NO cancelReason in protectedData. BUG: after a safety cancel the 'how to report (safety@)' guidance view doesn't show (modal closes) — the reason-note's 'we'll show you how' promise unfulfilled; fix queued. Dispute flow NOT live-reachable today (needs completed state/elapsed dates). | Verify booking-v2 activation | PM (autonomous) | No (test verification) |

| 2026-09-19 | Search result cards now show location/meta/tags (commit 0113a4d55, verified live — data existed; query wasn't requesting the publicData fields). Investigated 8 `recoverable-error`s on /s: Developer proved the cards are ISOMORPHIC (SSR==client) → NOT caused by this commit; they're PRE-EXISTING + recovered (non-breaking) — do NOT revert. Needs a browser componentStack to pinpoint (top suspect: pre-existing SSR currency formatMoney ICU; also third-party/extension). DEFERRED as lower-priority before-go-live polish. Separately: 18+ age gate VERIFIED working live (Journey 6 — under-18 blocked, adult allowed). | Search-card polish + verification | PM | No (test verification) |

| 2026-09-19 | Config-as-code (Tier 1) GROUNDWORK done (commit 76b244668, read-only exports + docs). Current ndstealth1-test no-code config captured in Git (`config/assets/*.json`: listing types/fields, user types/fields, access-control, branding, search schema) + `scripts/export-config-assets.js` (drift check) + `docs/config-as-code-status.md`. KEY FINDING: no write-API/CLI for the no-code config assets → reproducing them in a live env needs either Option A (port into `src/config/*.js`, code-authoritative — the only diffable/identical-across-envs path per path-to-live; needs a merge-precedence code change so Console doesn't override) or Option B (Console stays source of truth + manual checklist). Also flagged: process-tree drift on test (needs a `process pull` reconciliation before `ext/` is trusted as live source). | Config-as-code groundwork | PM (Option A/B decision queued for Neil) | Yes (launch-infra approach — pending Neil) |

| 2026-09-19 | UX Tester (Haiku) reliability: one sweep produced 3 PHANTOM findings (missing-logout, message-double-render, disabled-but-clickable button — all Developer-investigated, NONE real; + earlier date-picker false alarm). Wastes dev time. FIX: tightened `.claude/agents/ux-tester.md` to require hard evidence (URL/state/screenshot/quoted snapshot) + self-verification per finding; PM will verify tester findings before delegating fixes. Net sweep result GOOD: inbox/messaging/profile-editing flows all work + launch-ready. FLAG for Neil: if phantoms persist, consider a stronger tester model. | Agent-team quality | PM (brief tightened) | No (process; tester-model is a Neil option) |

| 2026-09-19 | Model onboarding wizard VERIFIED FUNCTIONAL by PM (reproduced the tester's claimed 'critical blocker' on the DETAILS tab). Reality: Next correctly ENABLES once all required fields are filled — the '4th phantom' was a required field the tester missed. REAL UX friction confirmed + queued: ~11 required model-attribute fields, Next disabled with NO indication of which is incomplete (tripped the tester; will trip models). TEAM DECISION for Neil: Haiku ux-tester has now produced 4 false findings (incl. a false 'critical blocker') even AFTER its brief was tightened → recommend a stronger tester model (e.g. Sonnet), or reserve it for scripted checks only. | Verification + agent-team quality | PM verified; tester-model escalation queued for Neil | Yes (team-config — pending Neil) |

| 2026-09-19 (Day 1) | Model onboarding wizard VERIFIED END-TO-END by PM (reliable walk as rt-model-01, no phantom): all 5 tabs work — About you → Your profile → Your rates → Your availability → Your portfolio (photo UPLOADER works). CONFIRMS RT-01 STILL LIVE: 'Submit for review' hard-gates on a mandatory Stripe payout modal — a model CANNOT reach 'under review' without completing Stripe KYC. Real model-onboarding launch-blocker; RT-01/02/08 (submit-review-golive-flow, APPROVED, awaiting Neil's build go) is the fix → strengthens the #1 queue item. | Onboarding verification | PM | No (verification) |

| 2026-09-19 (Day 1) | Onboarding required-field feedback SHIPPED + verified live (cobalt required-asterisks, optional kept, 'Still needed' checklist, 0 errors; commit 76c3e1c15) + card-meta short-label verified. Search filters VERIFIED: logic works across types (experience enum → 1; price range £100–200 → 1, £400–600 → 2) via URL params. Caveat: filter checkbox-CLICK UI couldn't be driven reliably via automation (inconsistent toggling — likely an artifact, stock FieldCheckboxGroup, logic works) → recommend a 30-sec human click-test to be 100%. | Verification (Day 1 max-progress) | PM | No |

| 2026-09-19 (Day 1) | Payment-DECLINE handling verified (decline card 4000…0002): clear 'Your card has been declined' error, stays on checkout, form preserved, retry enabled, NO transaction created. Correct. (Also confirmed the shared required-asterisk fix now shows on the checkout custom fields — app-wide, as intended.) | Payment edge-case verification | PM | No |

| 2026-09-19 (Day 1) | Decisions (Neil, decision cards): (1) BUILD RT-01/02/08 now — decouple submit from Stripe, Stripe at go-live (unblocks model onboarding). (2) Config-as-code OPTION A (code-authoritative port; careful merge-precedence, test-verified). (3) UX Tester model → SONNET (Haiku produced 4 phantoms even after brief-tightening). (4) ACTIVATE cancel/dispute emails (guided flex-cli push). ALSO: Neil's standing preference — ALWAYS present decisions via AskUserQuestion cards. | Batch-1 decisions | Neil (cards) | Yes (money/go-live: RT-01/02/08; process: config, emails) |

| 2026-09-19 (Day 1) | RT-01/02 SHIPPED (commits RT-20260802-01/-02, HEAD 6ee3634cb): 'Submit for review' NO LONGER gates on Stripe — models can submit without payout (unblocks the confirmed onboarding blocker). RT-02 confirmation banner enriched; RT-08 pendingApproval UX wired (needs Console Listing-approval ON — Neil). Part D (go-live payout gate) NOT built — real Sharetribe constraint: app can't publish a pendingApproval listing; native operator-Approve publishes IMMEDIATELY even w/o payout, so 'app publishes gated on payout' isn't app-implementable → needs an events-listener job (infra) + Neil's mechanism decision (carded). Also: EditListingPage.test.js has 9 PRE-EXISTING stale-test failures (wizard restructure) — cleanup item. | RT-01/02/08 build | PM (A/B/C built; Part D → Neil) | Yes (go-live payout mechanism — pending Neil) |

| 2026-09-19 (Day 1) | Config-as-code Option A PORTED + PM-reviewed (worktree branch worktree-agent-a57ac2e93c3b46d02, commits 8c2aa361f + 5080cedfb; NOT merged). Ported model-profile listing type + 18 listing fields + user types + 8 user fields into src/config/*.js; precedence change in configHelpers = code replaces hosted when defined, else hosted fallback (removes a dead dev-only union toggle). Precedence logic reviewed = clean/correct; build + 9/9 unit tests pass. NOT yet deploy-verified (correctness-critical) → PM to merge + deploy-verify carefully (wizard/listing/filters/signup render identically), revert-ready. Backend notes: keep Console field defs (search index), live env still needs some hosted assets for hasMandatoryConfigs, flex-cli search set on live. | Config-as-code build | PM (reviewed; deploy-verify pending) | No (test; reversible) |

| 2026-09-19 (Day 1) | Config-as-code Option A MERGED to main + DEPLOY-VERIFIED on test (commits 8368ebcea + b2d5d9116, cherry-picked from worktree; clean — RT-01 wizard change + decision-log preserved). Live verification: /s renders all 18 code-config listing-field filters + card meta with resolved enum labels; listing detail (Anais) renders 15/16 attribute fields + day/half-day/hourly rates, 0 console errors on load. 9/9 unit tests + full build pass. Code is now the authoritative config source (replace-not-union, all envs); hosted Console field defs kept in place for the search index. Verdict: PASS. | Config-as-code build | PM (verified) | No (test; reversible) |

| 2026-09-19 (Day 1) | RT-01 + RT-08 DEPLOY-VERIFIED live (full model journey walked as rt-model-02 Marcus, no Stripe connected). Completed profile→details→rates→availability→portfolio, clicked "Submit for review": NO payout/Stripe modal appeared (RT-01 ✅), listing went straight to /pending-approval (RT-08 ✅ → "Listing approval" is already ON in Console). Pending page copy already states "Payout details come later — you'll add them when you're approved, to start taking bookings" with a NON-blocking "Add payout details" banner — i.e. the deferred-payout posture is partly in place, directly supporting the Part D variant. | Submit/go-live flow | PM (verified) | No (test) |

| 2026-09-19 (Day 1) | Payout-deferral (Part D variant) — Neil GREENLIT the direction ("go with it if we can make it work") + spike delivered (docs/spikes/payout-deferral-feasibility.md): FEASIBLE with caveats. Stripe proven — for UK Custom accounts the bank gates ONLY payouts; identity alone enables charges, funds land in the MODEL's own Stripe balance (Stripe holds, not us → no money-transmitter exposure). Make-or-break unknown: whether Sharetribe's stripe-create-payment-intent accepts a charge-capable/no-bank provider (docs claim it won't — must test empirically on test env). Neil decisions (cards 2026-09-19): (1) TEST SETUP = Neil onboards a test model identity-only, PM runs the booking test; (2) 90-DAY RISK = accept + build reminders (day 30/60/75) + explicit copy (keep frictionless go-live). Still needs: empirical test pass → then build (UX/state only, no fund-flow change); lawyer eye on money-transmission; human dev review pre-launch. | Money / go-live | Neil (direction + 2 decisions) + PM | YES (money/public-facing — approved by Neil) |

| 2026-09-19 (Day 1) | UX proposal triage (PM, verified). model-onboarding-rewalk "BLOCKER: Your-profile Next disabled" = PHANTOM (test-harness artifact) — debunked by PM completing that exact step live (as rt-model-02) and advancing; root cause = tester set selects via JS, which doesn't register with Final Form (same issue PM hit — only browser_select_option enabled Next). journey-6-auth-account = CLEAN pass (age gate + login + all account pages working). Neither needs dev work. Fix: ux-tester brief now mandates REAL UI interactions (never JS value-setting) for forms + a re-fill-before-reporting check. Only real kernel: validation feedback on disabled Next — largely covered by shipped required-asterisks + ux-designer "Still needed" spec. | Team process / QA | PM (verified) | No |
| 2026-09-19 (Day 1) | Q3 MAKE-OR-BREAK TEST **PASSED** (live on test, PM-run). Marcus (rt-model-02) reached charges_enabled:true / payouts_enabled:false / no bank (Neil did Stripe identity-only, test DOB 01/01/1901; confirmed via stripe_account/fetch). PM booked him as client Priya (26–27 Sept, test card 4242): real initiate-privileged → stripe-create-payment-intent SUCCEEDED, card authorised, txn persisted (/order/6aaec560…) "booking request successful", pending model acceptance; fee split correct (£600 model / £90 15% / £690). ⇒ Sharetribe's "payout details required to process charges" doc claim is FALSE for the current engine — deferred-payout variant CONFIRMED feasible end-to-end. Funds accrue in the model's own Stripe balance until they add a bank (no money-transmitter exposure). Remaining: 90-day refund backstop (accepted, needs reminders), lawyer eye, human dev review pre-launch. Build now unblocked. | Money / go-live | Neil (onboarding) + PM (test) | YES (money — direction approved by Neil) |
| 2026-09-19 (Day 1) | Onboarding-split UX spike DONE (docs/spikes/onboarding-split-ux.md, research-only). Finding: a clean "skip bank / add later" UX is NOT achievable via the current Stripe HOSTED onboarding — for a GB individual the bank (external_account) is currently_due and our account link passes fields:'currently_due', so the bank step always appears (Neil's "close-the-window is the only exit" confirmed). The drop-bank options (external_account_collection:false / requirement exclude) are embedded-component-only, not exposed by Sharetribe's stripeAccountLinks.create. Recommended: (b) Stripe EMBEDDED Account onboarding (bank disabled for identity step) + (c) our OWN in-app bank form (Stripe.js token → sdk.stripeAccount.update({bankAccountToken}), Sharetribe-supported) for bank-later; both keep destination charges + 15% fee intact. Cheap interim to test first: Stripe's hosted "Save for later" link (unverified on the bank step). FLAGS for Neil (Day 2 decision): embedded path needs the marketplace Stripe PLATFORM SECRET KEY in our Express server + account_sessions/create (partial bypass of Sharetribe's Stripe layer) — architecture + secret-hygiene call; and this is safety-critical → human dev review before real users. Build still HELD pending Neil's flow/architecture decision. | Money / architecture | PM (spike) → Neil decision pending | Not yet (research) |
| 2026-09-20 (Day 2) | LAUNCH ONBOARDING/PAYOUT MODEL DECIDED (Neil, card): for launch, DO NOT ship deferred-payout — require models to complete Stripe (identity + bank/payouts) before they are discoverable/bookable. Models can build+edit a draft profile freely ("play around"); Stripe is required to submit/go-live. Rationale: simplest, lowest-risk, no new architecture/secret-key, and it ELIMINATES the 90-day stuck-funds risk. Deferred-payout parked as a documented post-launch optimisation (spikes retained; we know Sharetribe supports it). Gate placement (Neil, card): Stripe-BEFORE-submit. Implementation: **RT-01 REVERTED** (git revert 58a50d036 → commit 645f88a41) — handlePublishListing restores the stock payout gate (opens payout modal unless stripeConnected && no currently_due/past_due requirements); requirePayoutDetails reads code config payoutDetails:true (Config-A). Drafts still editable without Stripe; RT-08 approval + CheckoutPage backstop unchanged. Build passed; merged to main + deploying. Deferred-payout onboarding-split build (was held) is now DROPPED for launch. | Money / go-live | Neil (2 cards) + PM (impl) | YES (money/public-facing — Neil decided) |
| 2026-09-20 (Day 2) | Neil AUTHORISED running UX Journeys 2 (client discovery/search) + 3 (booking request/money-path) — previously only Journey 1 was authorised (ux-journeys.md gate). Authorisation is to WALK + report only; proposals return PENDING for Neil's sign-off (approval gate unchanged). PM tasking ux-tester (Sonnet, hardened brief) to walk both on test → ux-reports/proposals/{client-discovery,booking-request}-2026-09-20.md. | Product / QA scope | Neil (authorised) + PM | No (test) |
| 2026-09-20 (Day 2) | UX Journeys 2+3 findings TRIAGED (PM-verified each). VERDICTS — J2#1 sidebar attribute filters don't apply on click = REAL BLOCKER (verified: /s?pub_gender=has_any:female returns 3 correct results, but checkbox click never updates URL; recoverable-error render errors on /s suspected linked) → Developer fixing on branch fix/search-filter-checkboxes (PM deploy-verifies; MERGE pending Neil approval per UX gate). J2#6 recoverable-error on listing pages = REAL, likely same root cause. J2#5 footer "Post a new listing" stale label = REAL small copy fix. J2#2 Anais has no portfolio photo = REAL seed gap (not code). J2#4 footer Sharetribe boilerplate = REAL (Neil sets Console→Content→Footer). J2#3 homepage Featured-talent cards are static divs = REAL/known (discretionary: wire real listings). J3#1 rt-client-02 hits /no-transaction-rights = the INTENDED email-verification gate (Tom unverified) NOT a bug; but dead-end UX (no 'verify your email' path) = discretionary UX gap. J3#2 Stripe card postal rejects UK postcode = TEST-CARD ARTIFACT (stock unmodified CardElement, postal follows card country; 4242=US numeric) — confirm with real UK card at live-Stripe testing, no fix. J3#3 '2 days' billed for a 1-calendar-day range (26→27 and 27→28 both = 2 days) = CONFIRMED concern → Developer to investigate day-count semantics + date labeling (potential overcharge/confusion). J3#4 fee footnote misses 'model keeps 100%' + J3#5 client order tab titled 'Sale details' = REAL minor copy. All proposals remain PENDING (Neil signs off). | Product / QA | PM (verified) → Neil sign-off | No (test) |
| 2026-09-20 (Day 2) | RT-01 search-filter blocker FIX deploy-verified live (commit 5ea803139, root cause: omitLimitedListingFieldParams stripped model-attribute params on this single-listing-type marketplace; fix mirrors the pickListingFieldFilters activeListingTypes fallback). Verified: Female→?pub_gender=female 4→3 results, checkbox stays checked; Modelling categories→has_all:fashion 4→3. Shared code path → covers all 9 attribute filters. Remaining J2/J3 approved fixes (RT-03/05/06/07/09/10/11) building via Developer (isolated worktree); PM to deploy-verify + merge. | Product / QA | PM (verified) | No (test) |
| 2026-09-20 (Day 2) | J2/J3 approved-fixes BATCH built (Developer, isolated worktree) + cherry-picked to main (commits 0778e70dd, 1ad5c2321, dd63b7d77, 1c38357c1, fbd4d66bd; build+tests pass; deploying → PM deploy-verify pending). RT-11 client order browser title = client-facing; RT-10 checkout fee note = 'model keeps 100%'; RT-09 = NOT a bug (Sharetribe inclusive end-date, 2 full days correct) + clarification note added; RT-07 no-transaction-rights page now has verify-email hint + account-settings link; RT-03 homepage Featured-talent wired to REAL published listings via new GeneralLandingPage.duck (renders ListingCard, hides if empty). RT-05 (footer label) = Console-driven, moved to Neil (bundle w/ RT-04). RT-06 (authed hydration recoverable-errors) = DIAGNOSED systemic (SSR logged-out vs client authed; non-user-visible), real fix DEFERRED — needs tester componentStack re-capture + a scoped guard; do NOT touch index.js bootstrap. Pre-existing stale snapshot failures (ListingCard.test.js, ManageListingCard.test.js) flagged for separate cleanup. | Product / QA | PM + Developer | No (test) |
| 2026-09-20 (Day 2) | SAFETY-CLAIMS AUDIT (docs/safety-claims-audit-2026-09-20.md) — public copy vs v1 reality. 🔴 LAUNCH-BLOCKERS: (1) Safety/FAQ/landing claim clients/businesses are IDENTITY-VERIFIED — FALSE (only models are, via Stripe; SAF-03 client ID decided-in-scope but NOT built); (2) FAQ 'How do payments work?' still OLD 10%+5% model (R2-1, still live, contradicts 15%-only); (3) Safety/FAQ over-claim booking disclosure (nudity/people-present/usage-rights) but SAF-10 was REDUCED — those fields don't exist; (4) landing claims 'e-signed releases' + 'contract with image usage rights' — no such feature, not in scope. 🟠 safety@/support@roguetalent.COM bounce (should .co, R2-3 still live). NEEDS NEIL (queued for 3pm): DECISION A = client verification for v1 (PM rec: launch models-verified + honest client copy, add client ID post-launch) vs build SAF-03 now (Stripe Identity/manual); DECISION B = contracts/releases in v1 (PM rec: de-claim) vs build; + Console copy fixes (payments 10%+5%, nudity, .com emails) in one pass. | Legal / go-live / scope | PM (audit) → Neil decisions | Queued for Neil |
| 2026-09-20 (Day 2) | LIVE-LAUNCH RUNBOOK written (docs/live-launch-runbook.md) — ordered path to live marketplace + first £1, consolidating config-as-code-hand-list + path-to-live + repo-migration into an executable sequence. KEY: three LONG-LEAD items should START NOW (parallel to the v1 build) as they gate go-live with external lead time → (1) Stripe live account + business verification (KYB), (2) domain roguetalent.co pointing at a live host, (3) transactional email provider + domain auth (SPF/DKIM/DMARC). PM recommendation for 3pm: Neil kick these three off immediately — they're the real schedule driver for £1. | Go-live | PM (runbook) → Neil (start long-lead items) | Queued for Neil |
| 2026-09-20 (Day 2) | SAF-29 safety reporting flow BUILT + merged to main (commits 8a1b2f69c/6ed2f9e01/96234242f; build+tests pass; deploying → PM deploy-verify pending). Entry points: 'Report a safety concern' link on TransactionPanel (both parties) + open /report-concern route (works logged-out) + footer link. Form: category + description + who-about + prefilled contact; mandatory 'not a monitored emergency service — call 999' notice; same-working-day (Mon-Fri) expectation. Delivery: POST /api/safety-report → always structured [SAFETY_REPORT] logging + best-effort durable record on the REPORTER's privateData (Integration API, if creds present; never on the accused). GAP (infra): no email alert to safety@roguetalent.co — no mail transport in the stack → operator learns via logs/privateData only until a mail provider is added (= the email-provider long-lead item on the 3pm brief; then wire sendMail in server/api/safety-report.js). Needs the human safety review pre-go-live (SLA met once email wired; confirm open-logged-out form is OK / add rate-limit). | Safety-critical | PM + Developer (human review pre-go-live) | No (test) |
| 2026-09-20 (Day 2) | SAF-29 SSR crash FIXED (commit ecd7fd40a) — /report-concern was 500ing: SafetyReportForm spread submitError into FinalForm, colliding with final-form's read-only submitError getter. Fixed (destructure custom props from closure); reproduced + confirmed 200 via full local SSR build. PROCESS LESSON: Developer builds pass build-web+tests but do NOT catch SSR crashes — PM must SSR-check new routes (build-server + local render) before/at deploy-verify. SAF-13/14 BUILT + merged (commits 9b3cb03e3 + ea882c084): SAF-13 model-only 'no private-residence shoots' toggle in ProfileSettings → privateData.safety_boundaries.no_private_residence; SAF-14 silent server-side block in initiate-privileged (reads provider privateData via Integration API; generic 'booking not available' error, boundary never revealed; runs on speculative+real). No transaction-process change. FAIL-OPEN if Integration creds missing on Railway (loud log) — so SAF-14 ENFORCEMENT REQUIRES SHARETRIBE_INTEGRATION_CLIENT_ID/SECRET in the Railway server env (queue for Neil). All routes SSR 200 locally. Both safety-critical → human review pre-go-live. | Safety-critical | PM + Developer | No (test) |
| 2026-09-20 (Day 2, 3pm) | v1 SCOPE decisions (Neil, cards) — chose the COMPLETE path over lean: (1) BUILD client ID verification for v1 (SAF-03) — not de-claim; needs a mechanism (clients have no Stripe Connect → likely Stripe Identity standalone, or manual). (2) BUILD contracts / e-signed image-usage-rights for v1 — not de-claim (sizeable: contract generation + configurable usage rights + e-signature). (3) BUILD ALL safety before launch (SAF-11/17/25 included) — not fast-follow. (4) PM focus = client-verification build first. Implication (acknowledged): materially larger v1, first-£1 date moves out for a more complete/honest launch. PM starting client-ID feasibility spike now (mechanism + build plan + Neil dependency to enable Stripe Identity). Queued builds: client ID → contracts → SAF-11/17/25. | Scope / money / go-live | Neil (decided) + PM (executing) | Yes (scope — Neil decided) |
| 2026-09-20 (Day 2) | Client-ID-verification spike DONE (docs/spikes/client-id-verification.md). Mechanism = Stripe IDENTITY (standalone): server creates VerificationSession w/ platform secret key → hosted doc+selfie → webhook returns pass/fail; Stripe holds the ID images (GDPR win), we persist only a boolean on the client's user metadata (via Integration API, reuse safety-report pattern). Gate: add identity check to server/api/initiate-privileged.js alongside the email/permission gate; verify after signup, before first booking. Architecture flag (same as onboarding-split spike): needs the platform Stripe SECRET key in our Express server (only publishable exists today) + a signed webhook — Railway/.env only, never committed. PM decision (decide+drive): build the scaffolding now, ENV-GATED + fail-safe when keys absent (no secrets in code). NEIL DEPENDENCIES to activate+verify (queue for 3pm Day 3): (a) enable Stripe Identity in test mode, (b) add platform Stripe secret key + webhook signing secret to Railway/.env, (c) confirm Integration API creds on Railway, (d) approve ~£1.25/verification cost, (e) OK the 'platform secret key in our server' architecture. Human review pre-go-live (identity/PII/safety). | Money / security / go-live | PM (spike + will build scaffolding) → Neil (provision+confirm) | Yes (money/security — Neil to confirm) |
| 2026-09-20 (Day 2) | LIVE STRIPE STATUS confirmed (Neil checked dashboard) — account is LIVE (pk_live key), **business verification / KYB is DONE** ('Verify your business' ✅ in the setup guide), Connect enabled (in nav). The longest-lead launch item is essentially cleared. Remaining setup-guide steps (Test Connect / Build Connect integration / subscriptions / payments) are DIY-Connect steps we SKIP — Sharetribe manages the Connect integration via API keys. 'Create your Stripe profile' = optional public @handle identity, NOT needed for our destination-charge marketplace (skip; if ever created, use Rogue Talent branding + roguetalent.co, not the Uncommon Enterprises defaults shown). Live keys plug into Railway/Sharetribe at live-env build time (not now). Remaining long-lead items: domain email-provider auth + DAC7 tax_reporting preview confirm. | Go-live / money | Neil (verified) + PM | No (status check) |
| 2026-09-21 (Day 3) | Contracts/usage-rights spike DONE (docs/spikes/contracts-usage-rights.md). Recommendation: IN-APP CLICKWRAP (not third-party e-sign) — client selects usage terms (duration/channels/territory) at checkout → frozen onto transaction protectedData (rides existing request-payment update-protected-data, NO EDN change) → model agrees by ACCEPTING the booking (existing timestamped provider-attributed transition = the recorded acceptance) → both view/download a generated contract (print-based ContractPage, no PDF lib/deps). Build = 3 customer-role transaction fields (auto-render in checkout) + clickwrap checkbox + licence block on TransactionPage + ContractPage route. BLOCKING human deps: LAWYER drafts/signs off licence+release wording + confirms clickwrap legally sufficient (legal flow → human review pre-go-live); NEIL confirms the usage-rights taxonomy + "no third-party e-sign for v1". Gap: real contract wants LEGAL party names (only have display/account names today → ties to client-ID legal-name capture). PM plan: build the plumbing with a sensible default taxonomy + PLACEHOLDER legal copy (clearly flagged), lawyer wording swaps in later; sequence AFTER the client-ID build lands (avoid 2 concurrent legal/money builds). | Scope / legal | PM (spike) → Neil (taxonomy + clickwrap OK) + lawyer (wording) | Queued | 
| 2026-09-21 (Day 3) | SAF-03 client-ID verification (Stripe Identity) BUILT + merged to main (commits 8a8726352 + 5e8af3616; both builds pass; deployed — DORMANT/fail-safe until Neil provisions). PM security review PASSED: webhook sig = HMAC-SHA256 over timestamp.body + crypto.timingSafeEqual (constant-time) + replay-window; NO secrets in code (all process.env); stores ONLY boolean metadata.identity_verified (Stripe holds ID docs); gate fails OPEN + loud-logs when unconfigured (mirrors SAF-14) → does NOT block bookings on test. No stripe npm dep (REST+crypto — PM approved, per AGENTS.md). Gate chain in initiate-privileged: SAF-14 → SAF-03 → getTrustedSdk (email/permission gate native, unchanged); SAF-03 gates only real-initiate + userType=client. New: /verify-identity page (client verifyIdentity via window.Stripe, SSR-safe, 401 auth-gated logged-out — minor: could redirect to login), create-identity-session + stripe-identity-webhook routes. TO ACTIVATE (Neil): STRIPE_SECRET_KEY (sk_test_), STRIPE_IDENTITY_WEBHOOK_SECRET (whsec_), Integration creds on Railway, REACT_APP_IDENTITY_VERIFICATION_ENABLED=true, enable Stripe Identity + register webhook POST /api/stripe-identity-webhook (events verified/requires_input/redacted), approve ~£1.25/verif. Human dev review + privacy-policy/DPA (Stripe = doc processor) pre-go-live. End-to-end unverifiable without live keys. | Safety / money / security | PM (built+reviewed) → Neil (provision) | No (test; dormant) |
| 2026-09-21 (Day 3) | CONTRACTS / image-usage-rights v1 BUILT + merged (commits 76dcba049..da1d9f851; both builds pass; deploying). In-app CLICKWRAP (no third-party e-sign), NO EDN change, NO new npm deps (ContractPage uses browser print, not a PDF lib). Adds usage_duration/usage_channels/usage_territory (required customer transaction fields → freeze onto protectedData via existing request-payment) + required clickwrap checkbox at checkout (licenceAgreedByCustomer+version) + model-agrees-on-Accept copy + UsageLicenceSection on TransactionPage + print-optimised /contract/:id ContractPage (auth-gated, SSR-safe 401 logged-out). Placeholder legal copy ALL marked 'DRAFT — pending legal review' (locations in en.json: StripePaymentForm.licence*, UsageLicenceSection.*, ContractPage.*). GAPS/deps: (1) LAWYER must supply real licence/release wording + confirm clickwrap sufficiency (pre-go-live); (2) legal-name gap (uses display/company name, not verified legal names → ties to SAF-03); (3) Developer flagged 'Console mirror' for the 3 usage_* fields — PM assessment: likely NOT needed since Config-A is code-authoritative + shoot_* (required) renders at checkout from code; VERIFYING via deploy (checkout render test). Human review pre-go-live. | Scope / legal | PM (built+reviewed) → lawyer (wording) + Neil (taxonomy confirm) | No (test; DRAFT copy) |
| 2026-09-21 (Day 3) | Contracts deploy-verify outcome: build-web + build-server PASS, SSR smoke clean (/, /s 200; /contract/:id 401 auth-gated, no crash), code-reviewed. CONSOLE-MIRROR QUESTION RESOLVED = NOT needed: usage_* are structurally identical to shoot_* (required customer transaction fields in configListing.js) and shoot_* demonstrably render at checkout from CODE (Config-A authoritative; PM completed bookings with shoot_* Days 1-2) → usage_* render the same from code, no Console/flex-cli step for rendering (they're order fields, not search filters). IN-CHECKOUT VISUAL confirmation (seeing usage_* fields + clickwrap on the checkout page) NOT captured — blocked by react-dates end-date picker being automation-unfriendly (test-tooling artifact, NOT a product bug; real users click dates fine — full bookings completed this way Days 1-2). To finish: a human (or a clean booking walk) eyeballs the checkout usage fields + clickwrap — folded into the pre-launch human review. | Product / QA | PM (verified high-confidence; visual pending) | No (test) |
| 2026-09-21 (Day 3) | SAF-25 "Safety & respect" review dimension BUILT + merged (option A, Neil-approved; commits bc3aa62dc/9e99a5130/478d03696; both builds pass). EDN: added :action/update-protected-data to the 4 review transitions (role-keyed safetyRespectRatingByCustomer/Provider so the two reviewers don't overwrite); locally validated (flex-cli process --path describe, NO marketplace write). Code: reviewSafety.js helper + 2nd FieldReviewRating (REQUIRED — Developer flag: could make optional to protect completion rate, 1-line change) + onSubmitReview threads to protectedData + display in Reviews + ActivityFeed (party/operator-visible, NOT public). Absence-guarded (older reviews show nothing). NEIL: the EDN needs flex-cli process push + update-alias (BUNDLE with the emails activation — one push); until pushed, code is forward-compatible/harmless. Human review pre-go-live. This COMPLETES the decided v1 safety set (SAF-01 via Stripe/10/11/13/14/17/24/29/38 + client-ID SAF-03) + contracts → v1 FEATURE BUILD COMPLETE. | Safety-critical | PM (built+reviewed) → Neil (push EDN) | No (test) |
| 2026-09-21 (Day 3) | Contracts REVISED to Neil's new direction (2026-09-21) + merged (commits 144aa8048 + 63b82e3b7; both builds pass): ONE standard broad+perpetual "Rogue Talent Content Licence & Model Release" for EVERY booking — removed the per-booking usage_duration/channels/territory selectors entirely (+ helpers/rendering; zero refs remain). Client agrees via clickwrap at checkout (licenceAgreedByCustomer+version); model agrees on Accept. ContractPage (/contract/:id) renders the single contract (plain-English summary + 12 clauses from docs/legal/rogue-talent-content-licence-DRAFT.md, in en.json Contract.*) + a per-booking Schedule (client/model/dates/location/ref/fee), guarded for legacy txns. TransactionPage shows standard-licence summary + Schedule. All copy DRAFT — pending SOLICITOR (Neil to arrange; draft + 7 review points sent). No EDN, no deps, checkout intact. TIDY-UP (low pri, Neil/backend): Console still has the 3 usage_* fields defined (unused now; harmless since Config-A authoritative) — remove for tidiness. Model-toggle = explicitly post-launch. | Scope / legal | PM (built+drafted) → lawyer (wording) | No (test; DRAFT) |
| 2026-09-21 (Day 3) | EMAIL PROVIDER = Postmark (Neil delegated choice to PM; PM recommended Postmark for best-in-class TRANSACTIONAL deliverability — approval/booking/safety emails must not spam; ~$15/mo; purpose-built not marketing). Neil to set up: create account, verify roguetalent.co via GoDaddy DNS (DKIM + Return-Path; don't duplicate existing SPF; add DMARC), hold API token for live env. Used for OUR server-sent emails (SAF-29 safety alerts to safety@, model approval-flow under-review/declined, custom notifications); Sharetribe sends its own booking-v2 txn notifications (from-address set in Console separately). Also: Neil completed Stripe rep IDENTITY verification (submitted, awaiting Stripe review) → KYB essentially fully done. | Go-live / infra | Neil (delegated) + PM (recommended) | No | 
| 2026-09-21 (Day 3) | Contract benchmark RESEARCH done (docs/research/model-contract-benchmark.md; 10 sources incl. BFMA/AMA UK booking T&Cs + live agency terms + release forms + buyout guides). Findings: UK agencies license usage NARROW + TIME-LIMITED (default UK/1-image/1-medium/1yr) and re-charge per extension (usage = their recurring revenue); the all-media/perpetual grant is called 'full buyout', priced at a premium/refused → our single-flat-fee perpetual buyout is an EVIDENCE-BACKED disruption. RISK: not that the licence is too broad, but that removing protection-by-expiry leaves the draft THIN on model protections a lawyer expects. GAPS to add pre-solicitor: (1) 🔴 AI-training/synthetic-likeness carve-out (silent — #1 2026 red flag), (2) revoke-on-material-breach + takedown (currently irrevocable/no exit), (3) portfolio+credit, (4) competitor/category-conflict, (5) consideration recital. +3 solicitor Qs (AI, revocability-on-breach, consumer-fairness of perpetual single-fee where model may be consumer/sole-trader). PM recommended folding in AI carve-out + consideration recital + portfolio/credit + revoke-on-material-breach; flag category-conflict for solicitor. Awaiting Neil's confirm on the set → then update draft. | Legal / research | PM → Neil (confirm additions) | No |
| 2026-09-21 (Day 3) | Contract v3 DONE + sent to Neil (commit 9f05c7006; docs/legal/rogue-talent-content-licence-DRAFT.md). Neil approved "add them all and send v3" → folded in ALL benchmark additions: (5A) AI/machine-learning/synthetic-likeness carve-out — no training AI models or generating deepfakes of the model without separate written consent (ordinary editing under cl.2 still fine); (5B) revoke-on-material-breach + takedown — softens cl.2 "irrevocable (subject to 5B)": model may revoke only the breaching use, only if client breaches 2A/5/5A and fails to remedy after written notice, compliant use unaffected; (2A) portfolio/show-reel retained + may ask to be credited; (3) consideration recital — fee agreed as full/fair consideration for a perpetual "full buyout", entered into freely (defends fairness). Plain-English box synced. +4 solicitor Qs (8 AI robustness, 9 revocability-on-breach holds in E&W, 10 consumer-fairness of perpetual single-fee where model may be consumer/sole-trader, 11 competitor/category-conflict decline at launch vs post-launch toggle). Still DRAFT — Neil arranges solicitor review before use; in-app en.json Contract.* copy stays at the earlier draft until the lawyer returns (one sync pass then). | Legal | PM (built) → lawyer (Neil to arrange) | No (test; DRAFT) |
| 2026-09-21 (Day 3) | POSTMARK domain auth COMPLETE + mail transport BUILT. Neil verified roguetalent.co in Postmark (DKIM + Return-Path both Verified). DMARC: PM DNS-checked and caught a DUPLICATE — a stale GoDaddy default record (`p=quarantine; rua=…onsecureserver.net`) coexisted with Neil's new `v=DMARC1; p=none; rua=mailto:hi@roguetalent.co` → two DMARC records = spec-invalid, DMARC ignored. Neil deleted the GoDaddy one; PM re-verified via 2 resolvers = exactly one record now. SPF left untouched (Postmark uses its own Return-Path, no SPF change). BUILD: `server/api-util/mailer.js` = Postmark REST transport over Node https (NO postmark npm dep, matches stripeIdentity pattern), env-gated on POSTMARK_SERVER_TOKEN, FAIL-SAFE (sendMail resolves {skipped/sent:false}, never throws — a mail failure can never fail the calling request). Wired into SAF-29 `server/api/safety-report.js`: best-effort alert to safety@roguetalent.co (SAFETY_ALERT_EMAIL override) alongside the existing log + privateData capture; response now returns {reportId, persisted, emailed}. Verified fail-safe locally (unconfigured → skipped; both modules load). Neil added POSTMARK_SERVER_TOKEN to Railway + redeployed (predated the code) → pushed 23b0ec36e; Railway rebuilt WITH the wiring. SMOKE TEST PASSED (live endpoint, logged-out test report): response {reportId, persisted:false, emailed:TRUE} → new build live, token found, Postmark ACCEPTED the send. Delivery-to-inbox (vs bounce) is Neil's remaining confirm via Postmark Activity + safety@ inbox (create the safety@roguetalent.co alias if Activity shows a bounce). NUANCE: Postmark server is in Test mode / pending approval — alerts to safety@roguetalent.co are ON-DOMAIN so should deliver pre-approval; sending to arbitrary external recipients needs "Request approval" (go-live step). DMARC hardening p=none→quarantine→reject after reports confirm alignment (queued). Env for Neil (done ✅): POSTMARK_SERVER_TOKEN. Optional: POSTMARK_FROM_EMAIL (default no-reply@roguetalent.co), POSTMARK_MESSAGE_STREAM (default outbound), SAFETY_ALERT_EMAIL (default safety@roguetalent.co). Postmark = OUR server mail only; Sharetribe booking-v2 notifications still sent by Sharetribe (Console from-address). | Infra / safety | PM (built) + Neil (provisioned token) | No (test) |
| 2026-09-21 (Day 3) | Postmark SAF-29 alert DELIVERY CONFIRMED end-to-end: Postmark Activity shows Processed→Delivered to safety@roguetalent.co in 1s, tag safety-report, 0% bounce/spam, stream=outbound (matches mailer default). Email provider fully done+verified; only remaining Postmark item = Request approval (go-live, for EXTERNAL recipients). MODEL-APPROVAL-EMAILS investigation (PM): model approval = Sharetribe NATIVE user state (pending-approval→active/banned, userHelpers.isUserAuthorized). The approval emails are Sharetribe-native (sent from Sharetribe infra, content in Console) — NOT our server. DECISION: do NOT build approval emails on Postmark (would duplicate/conflict with native + needs a Console→server trigger we lack). Approved-email + operator new-user-needs-approval alert = CONSOLE config task for Neil (enable/brand, set roguetalent.co from-address). Declined/ban email has no native equivalent → DEFER (operator emails manually at launch volume; automating needs a trigger). Postmark transport stays for server-originated mail Sharetribe doesn't send (SAF-29 + future bespoke). No code needed. | Infra / architecture | PM (investigated) → Neil (Console) | No |
| 2026-09-21 (Day 3) | Postmark APPROVED + live (external sending unlocked — last email go-live gate cleared; free tier 100/mo, upgrade when volume needs). CLIENT VERIFICATION scope decided (Neil): verify the INDIVIDUAL via Stripe Identity (automated, accountable-human safety anchor, covers sole-traders) + the BUSINESS via a MANUAL Companies House check folded into the existing client manual-approval step (zero build). "Businesses are verified" copy KEPT (now honestly backed by the manual CH check; Neil rejected the reworded softer copy as weak). Client-ID Stripe steps still valid; provisioning paused pending Neil (he has the click-by-click). | Scope / safety | Neil (decided) + PM | Yes (scope — Neil) |
| 2026-09-21 (Day 3) | ACCOUNT STATUS LIFECYCLE spec written (docs/specs/account-status-lifecycle.md) — Neil's proposal + refinements; DRAFT SPEC, awaiting Neil sign-off, NO CODE until signed off. One linear flow shared by models+clients, two gates: Gate A = manual review (models=portfolio QUALITY control, strict early; clients=Companies House business legitimacy), Gate B = verification (models=Stripe identity+bank; clients=Stripe Identity). 5 states: Draft(grey)→Pending approval(amber)→Approved(cobalt)→Verified(green) + Rejected/Suspended(red). Status = LINEAR review-first fn: submitted&&!approved→Pending (even if verified early — banked); approved&&!verified→Approved; approved&&verified→Verified. Verified is a LIVE computation (Stripe lapse → drops out → hidden/unbookable). Enforcement: models visible+bookable ONLY when Verified; clients book ONLY when Verified. **RE-OPENS/SUPERSEDES RT-01**: drops "Stripe before submit" — verification NOT a prereq to submit (submit=quality gate first, verify after; "bookable only when Verified" is the backstop). NEW: (a) Approved→verify email NUDGE sequence via the new Postmark transport (needs a trigger build); (b) client company-registration-number field (Draft) for the CH check; (c) RETIRE the old per-card VerifiedBadge + id_verified metadata (redundant since all visible models are Verified). Open impl choice (PM leaning user-state as Gate A source of truth). ux-designer tasked (parallel) → exact tokens incl. NEW amber + rt-status badge spec (docs/specs/account-status-badge-design.md). | Product / scope / UX | Neil (proposed) → PM (spec) → Neil sign-off before build | Yes (scope — sign-off pending) | 
| 2026-09-22 (Day 4) | ACCOUNT STATUS — STEP 1 (foundation) built + merged + deploy-verified (main 8335b227a; routes 200). Amber token ramp (built around existing --status-warning, zero visual change), rt-status AccountStatusBadge component, getAccountStatus helper (+18 tests), badge on ProfileSettingsPage (self-view). Note: a not-yet-approved model reads "Draft" there until the own-listing signal is wired (ProfileSettingsPage doesn't load it) — small follow-up. Git note: the concurrent Developer's checked-out branch briefly captured a PM commit (shared working dir) — resolved via ff-merge; use worktree isolation for concurrent builds next time. | Product / UX | PM (built+reviewed+merged) | No (test) |
| 2026-09-22 (Day 4) | ACCOUNT STATUS — STEP 2 (onboarding rewire + Verified-only visibility) built + PM-reviewed + merged to main DORMANT behind REACT_APP_ACCOUNT_STATUS_FLOW_ENABLED (default OFF = today's exact behaviour; main 89fd90d6e; build/SSR/191+21 tests pass). Neil approved the §3 approach + all 3 decisions. Pieces: modelVisibility.js reconcile core (Verified⟺listing published, idempotent, fail-safe, live-lapse close), Stripe Connect account.updated webhook + own-session reconcile (/api/reconcile-own-listing, pinged by ManageListingsPage), providerVerifiedGate in initiate-privileged (fail-open unconfigured, fail-closed on lookup gap), handlePublishListing flag-gated to drop RT-01. SAFETY-CRITICAL → pre-go-live human review required. CUTOVER gated (do NOT flip flag until): listing-approval ON in Console (Neil, #1), Integration creds on Railway, Connect webhook registered + STRIPE_CONNECT_WEBHOOK_SECRET, residual-risk#1 resolved, human review. RESIDUAL RISK #1 (biggest): Connect webhook acct_→user mapping unconfirmed (Integration API has no Stripe endpoint) → lapse-while-away may not hide model until next dashboard load (backstopped by booking gate + Stripe capture). PM rec to resolve pre-cutover: store acct_ id on listing publicData at connect + webhook queries listing by it (removes the metadata guess). Steps 3-5 (client company field, verify-nudge emails, retire old badge) still queued. | Product / safety / scope | Neil (approved) + PM (built+reviewed+merged dormant) | No (test; dormant) |
| 2026-09-22 (Day 4) | ACCOUNT STATUS — STEPS 3, 4, 5 + webhook-fix built (isolated worktrees), PM-reviewed, merged to main (build/SSR pass; 67/67 account-status server tests green on real checkout). STEP 3: client company_registration_number user field (client-scoped, public, optional, config-as-code; operator reads via Console/Integration; pairs with existing company_name) — not flag-gated (additive). STEP 5: retire old per-card/profile VerifiedBadge on ListingCard+UserCard, FLAG-GATED (hidden only when REACT_APP_ACCOUNT_STATUS_FLOW_ENABLED ON; component kept) + fixed ProfileSettingsPage badge accuracy (loadData now fetches own listing, SSR-safe, Draft fallback). STEP 4: Approved→verify email NUDGE — POST /api/cron/verify-nudge (CRON_SECRET timingSafeEqual guard, dryRun), sweep finds Approved-unverified (models via listing!=published, clients via identity_verified!=true), dedup lastVerifyNudgeAt + cap verifyNudgeCount<3 + 48h cooldown, 2 Postmark copy variants; DORMANT until Integration+Postmark+CRON_SECRET set; cron trigger = Neil provisioning (daily POST + X-Cron-Secret). WEBHOOK-FIX (residual-risk #1 resolved): durable acct→model mapping — stamp publicData.stripeAccountId on own-session reconcile (idempotent), Connect webhook resolves via listings.query pub_stripeAccountId (metadata guess = fallback only); narrow benign gap (first account.updated before model ever loads dashboard) documented. → ACCOUNT-STATUS LIFECYCLE BUILD FUNCTIONALLY COMPLETE (all dormant/flag-gated; nothing changes for users until cutover). Safety-critical bits still need pre-go-live human review; cutover still needs Neil (listing-approval ON + provisioning). Next: ux-designer full-site cosmetic review (Neil's requested capstone). | Product / safety / scope | PM (built+reviewed+merged) | No (test; dormant) |
| 2026-09-22 (Day 4) | UX capstone: full-site COSMETIC REVIEW delivered (docs/design/cosmetic-review-2026-09-22.md; 10 quick wins / 8 medium / 5 larger, each COS-## + file + token). Verdict: DS strong where applied; uneven rollout — money/trust screens (profile/booking, checkout, transaction panel) still stock pre-DS template = the 3 "Larger" rebuilds (proposed follow-on project). Neil green-lit "quick wins + global headings" → IMPLEMENTED + merged to main (10 commits, one per COS id, CSS-token-only, build+SSR pass): COS-02 (global h1-h3 → Bricolage display, sizes snapped to DS scale — highest reach), COS-06 (Inbox unread dot red→cobalt, brand-rule fix), COS-28 (checkout draft-licence notice red→amber, brand-rule fix), COS-25/34 (booking + dashboard price → Bricolage extrabold), COS-33/08 (heavy 50px glow shadows → DS --shadow-* scale, sitewide re-alias), COS-21 (sort control rt-chip hover), COS-24 (bio 1s→DS motion), COS-07 (icon stroke → ink-600 + softer radius). PM deploy-verifying + visual pass pending. COS-01 (footer placeholder "add your slogan here" ×2, most-visible unfinished item) = Neil Console fix; slogan chosen = "Professional talent, booked direct." Remaining medium/larger held for Neil's steer. | UX / polish | ux-designer (review) + PM (built+merged) + Neil (scope + slogan) | No (test; cosmetic) |
