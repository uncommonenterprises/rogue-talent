# See http://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
node_modules

# testing
coverage

# production
build

# misc
.DS_Store
.env
npm-debug.log
yarn-error.log

# Local working artifacts (not for the repo)
backups/
docs/discovery-model-fields.md

# Seeded test-user roster (contains a password) — never commit
.test-accounts.json

# Playwright MCP session artifacts (console logs, traces) from PM runs
.playwright-mcp/

# Generated PM review reading-view — derived from the proposals .md + screenshots,
# ~5MB per run and regenerable via scripts/ops/build-review-html.js. The markdown
# and screenshots are the record; the HTML is not committed.
ux-reports/review/
